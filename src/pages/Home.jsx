import { CardWrapper } from "../features/cards/CardWrapper";

export const Home = () => {
    console.log("Home component rendered");

    return (
        <div>
            <CardWrapper />
        </div>
    );
};
