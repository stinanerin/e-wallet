export const Header = () => {
    return (
        <header className="w-full bg-primary p-3 bg-gradient-to-b from-primary to-bkg">
            <h1 className="text-center text-xl sm:text-3xl uppercase font-bold tracking-tight text-text_contrast">
                E-wallet
            </h1>
        </header>
    );
};
